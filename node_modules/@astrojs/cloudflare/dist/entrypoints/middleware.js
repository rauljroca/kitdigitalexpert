export const onRequest = (context, next) => {
    if (context.isPrerendered) {
        // @ts-expect-error
        context.locals.runtime ??= {
            env: process.env,
        };
    }
    return next();
};
